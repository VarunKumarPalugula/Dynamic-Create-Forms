import { IsEllipsisActiveDirectiveDirective } from './is-ellipsis-active-directive.directive';

describe('IsEllipsisActiveDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new IsEllipsisActiveDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
